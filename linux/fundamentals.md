Operating System:
-----------------
operating system is an intermediate software layer that acts as a bridge between  user and hardware.it performs process management, memory management, device management and network management because application is also a software program. it can talk to the operating system which is a software layer and this operating system depending on the program it allocated the cpu, memory and required hardware resources.<br>
There are other operating system as well but most popular Operating Systems are:<br>
- Linux
- Windows
- Mac OS

UNIX is the first OS that was developed in 1960 and it is open source software.
In 1970, Minix OS was developed but minix is not completely open source. it was partially open source.

Microsoft released their windows OS in 1980. this rich graphical user interface(GUI) made communication to the users with the hardware super simple. they don't even need any commands which wasrequired for unix and minix.

In 1990, linux torvalds worked on development of linux. this was the first popular open source OS although unix was also open source OS, this was compeetitive with windows 80 linux became the new popular choice for the developers and also for the companies.

Linux is a free open source and it is super secure compared to other operating system.

Linux OS main componenet is linux kernel so interaction with the hardware such as process, memory, disk and network management is handled through the linux kernel.

On the top of the hardware, there is a linux kernel, system utilites, system libraries and system binaries such as CLI or GUI. For linux, it's a CLI so these are the four things that's what wecall as a Linux OS.

                       -------------------------------------------
                       |          shell (CLI or GUI)             |
                       -------------------------------------------
                       |          system libraries               |
                       -------------------------------------------
                       |          system utilites                |
                       -------------------------------------------
                       |          Linux Kernel                   |
                       -------------------------------------------
                       |          Hardware                       |
                       -------------------------------------------

Linux Distributions:
--------------------

- Ubuntu
- RedHat
- Fedora
- Kali
- Debian
- Centos
- Arch
- Alpine

Package Manager:
----------------
Package managers are help you deploy or install a dependency like python. it helps you to upgrade of the new version of the package, delete the version of the package or even maintain the packages.<br>
Ex: python, java <br>
Using package managers, you can install python, java and you can remove from the linux machine or upgrade java id continuously maintain versions of java.

                                 
